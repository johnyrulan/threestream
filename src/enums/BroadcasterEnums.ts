export enum BroadcasterRole {
    Presenter = 'presenter',
    Moderator = 'moderator',
    Attendee = 'audience',
}

export enum BroadcasterCodec {
    H264 = 'h264',
    MP4 = 'mp4',
}

export enum BroadcasterType {
    camera = 'camera',
    screenshare = 'screenshare'
}
